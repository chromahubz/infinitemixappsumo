[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg-full"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "echo 'Verifying lib/task-store.ts exists...'",
  "ls -la lib/task-store.ts",
  "npm run build"
]

[start]
cmd = "npm start"
